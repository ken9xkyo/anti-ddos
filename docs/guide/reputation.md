# Trang Reputation

Trang `Reputation` da retired trong RBAC `admin`/`user` khong con tenant.

Threat Feed/Reputation khong con navigation va khong con API user-facing trong dashboard scope hien tai. Manual blacklist van duoc quan ly tai tab `Blacklist`; feed/reputation neu can se duoc thiet ke lai trong mot feature rieng.
